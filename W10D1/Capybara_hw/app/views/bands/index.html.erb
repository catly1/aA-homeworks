<a href="<%= new_band_url %>">
    Add Band
</a>

<h1>List of Bands:<h1>
 <ul>   
 <% @bands.each do |band| %>
      <div class="band-index-item">
        <a href="<%= band_url(band) %>"><%= band.name %></a>
<% end %>
</ul>